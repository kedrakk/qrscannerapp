import 'package:flutter/material.dart';

class ContactFieldPage extends StatelessWidget {
  const ContactFieldPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Contact"),
    );
  }
}
